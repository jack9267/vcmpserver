#ifndef _SQSOCKETFUNCS_H
#define _SQSOCKETFUNCS_H

#include "SquirrelMain.h"

class CSQSocketFuncs
{
public:
	static _SQUIRRELDEF( New );
	/*static _SQUIRRELDEF( Delete );

	static _SQUIRRELDEF( Connect );

	static _SQUIRRELDEF( Start );
	static _SQUIRRELDEF( Stop );

	static _SQUIRRELDEF( SetNewConnFunc );
	static _SQUIRRELDEF( SetLostConnFunc );

	static _SQUIRRELDEF( Send );*/
};

#endif
