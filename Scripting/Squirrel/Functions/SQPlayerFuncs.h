/*
 * SQPlayerFuncs.h
 * Edited: 26/3/2009
 */


#ifndef _SQPLAYERFUNCS_H
#define _SQPLAYERFUNCS_H

#include "SquirrelMain.h"


class CSQPlayerFuncs
{
public:
	//static _SQUIRRELDEF( GetID );
	static _SQUIRRELDEF( FindPlayer );
	/*static _SQUIRRELDEF( GetNick );
	static _SQUIRRELDEF( GetIP );
	static _SQUIRRELDEF( GetPing );

	static _SQUIRRELDEF( GetHealth );
	static _SQUIRRELDEF( SetHealth );
	static _SQUIRRELDEF( GetArmour );
	static _SQUIRRELDEF( SetArmour );

	static _SQUIRRELDEF( GetScore );
	static _SQUIRRELDEF( SetScore );
	static _SQUIRRELDEF( IncScore );
	static _SQUIRRELDEF( DecScore );

	static _SQUIRRELDEF( GetClass );
	static _SQUIRRELDEF( GetTeam );
	static _SQUIRRELDEF( GetSkin );
	static _SQUIRRELDEF( SetAnim );
	static _SQUIRRELDEF( GetWeapon );
	static _SQUIRRELDEF( SetWeapon );
	static _SQUIRRELDEF( GetControls );

	static _SQUIRRELDEF( GetCash );
	static _SQUIRRELDEF( SetCash );
	static _SQUIRRELDEF( GiveCash );
	static _SQUIRRELDEF( TakeCash );

	static _SQUIRRELDEF( GetVehicle );
	static _SQUIRRELDEF( PutInVehicle );
	static _SQUIRRELDEF( IsPassenger );

	static _SQUIRRELDEF( IsAdmin );
	static _SQUIRRELDEF( IsSpawned );
	static _SQUIRRELDEF( IsFrozen );
	static _SQUIRRELDEF( GetSpectating );
	static _SQUIRRELDEF( ToggleControls );

	static _SQUIRRELDEF( IsMuted );
	static _SQUIRRELDEF( Mute );

	static _SQUIRRELDEF( IsIgnoredBy );
	static _SQUIRRELDEF( Ignore );
	static _SQUIRRELDEF( Unignore );

	static _SQUIRRELDEF( IsSyncBlocked );
	static _SQUIRRELDEF( SetSyncBlocked );
	static _SQUIRRELDEF( IsSyncBlockedTo );
	static _SQUIRRELDEF( SetSyncBlockedTo );

	static _SQUIRRELDEF( GetPos );
	static _SQUIRRELDEF( SetPos );
	static _SQUIRRELDEF( SetInterior );
	static _SQUIRRELDEF( GetAngle );

	static _SQUIRRELDEF( SetMarker );
	static _SQUIRRELDEF( RemoveMarker );
	static _SQUIRRELDEF( SetLocalMarker );
	static _SQUIRRELDEF( RemoveLocalMarker );*/
};

#endif
